apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: covidomysqlpvc
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteMany
  storageClassName: covidostorageclass  
